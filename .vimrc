" Enable syntax highlighting
syntax on

" Show line numbers
set number

" Enable mouse support in all modes
set mouse=a

" Enable auto and smart indentation
set autoindent
set smartindent

" Show matching brackets when cursor is on one
set showmatch

" Enable line wrapping (optional, remove it if you prefer horizontal scrolling)
set wrap

" Enable file type detection and plugin support
filetype on 
filetype plugin on 
filetype indent on

" Set a more comfortable backspace behavior
set backspace=indent,eol,start

" Set tabs to 4 spaces (good for code readability)
set tabstop=4 
set shiftwidth=4
set expandtab

" Enable incremental serach 
set incsearch

" Highlight search matches
set hlsearch
